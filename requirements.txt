# Versions checked against PyPI on 16 September 2026.

python-dotenv==1.2.3
# Windows has no system IANA timezone database, so Europe/London is
# unavailable to zoneinfo without this.
tzdata==2026.4

# Language models. Gemini is the default: Groq answers a single request
# faster, but its free tier allows 8000 tokens per minute, which a tool
# calling conversation exhausts. See docs/ENGINEERING_LOG.md.
google-genai==2.23.0
groq==1.7.0

# Web server
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5

# Voice: Deepgram speech in and out, and the pipeline around it. The call
# itself travels over a WebSocket; the webrtc extra is kept for local use.
pipecat-ai[deepgram,webrtc,runner]==1.10.0

# Tests
pytest==9.1.1

# Deliberately absent: chromadb and sentence-transformers. The plan was
# embedding retrieval over the practice documents. The corpus turned out to
# be eight short documents with a fixed vocabulary, where plain keyword
# retrieval passes every retrieval test and returns nothing rather than a
# weak match. Embeddings would have added a download of about 2.5 GB,
# mostly PyTorch, for no measured gain. See src/sophia/knowledge.py.
